/**
 * Playback Service — placeholder
 * Audio background playback is handled directly by expo-av in soundLab.ts
 * with staysActiveInBackground: true and playsInSilentModeIOS: true.
 */
export async function PlaybackService() {}
